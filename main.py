from plyer import notification

notification.notify(title="TAKE A BREAK FROM CODING",
                    message="Taking small breaks is essential",
                    app_icon='icon/not.ico',
                    timeout=60

)